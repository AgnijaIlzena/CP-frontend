import { Link } from "react-router-dom";

const Navigation = () => {
  return (
    <Link className="navigation" to="/continents">
      Go to Continents
    </Link>
  );
};

export default Navigation;
